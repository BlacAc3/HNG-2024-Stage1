{
    "builds": [{
        "src": "my_api/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "my_api/wsgi.py"
        }
    ]
}